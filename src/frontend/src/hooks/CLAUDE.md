# Hooks

Custom React hooks for cross-cutting concerns.

## Contents

- **useColorScheme.ts**: Returns the active OS color scheme (`'light'` or `'dark'`) and reacts to changes.

## Conventions

- Each hook is a named export from its own file.
- Each hook has a co-located `.test.ts` file.
